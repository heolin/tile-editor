Pliki referencyjne TMX/TSX pobrane z `mapeditor/tiled/examples`.

Służą do jednego: sprawdzenia, czy kodek XML czyta i zapisuje to, co naprawdę
pisze Tiled. Korpus w `examples/` jest w całości w JSON, więc bez tych plików
kodek XML nie miałby na czym być zweryfikowany.
