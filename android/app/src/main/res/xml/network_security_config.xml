<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to a tile-editor server running on the same device, normally
  inside Termux, over plain HTTP on loopback. Android 9 and later block
  cleartext by default, so loopback is allowed here and nothing else is:
  everything outside these hosts still requires TLS.

  Connecting to a server on another machine over the network would need that
  address added here as well.
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false" />
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
    <!-- The host machine as seen from the Android emulator. -->
    <domain includeSubdomains="false">10.0.2.2</domain>
  </domain-config>
</network-security-config>
